# H Computer-Use Agents API key (bearer token). Get one at:
#   https://platform.hcompany.ai/settings/api-keys
# The SDK's Client() picks this up automatically. EU region is the default host
# (https://agp.eu.hcompany.ai); the key is scoped to your organization.
HAI_API_KEY=
